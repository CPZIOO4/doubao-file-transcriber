# 更新日志

## 1.2.0-preview.1 — 2026-09-23

- 音频先按总时长均匀规划分段，再在目标点附近寻找停顿；每段不超过 40 秒，连续讲话的切点保留 0.8 秒重叠。
- 每段成功识别后立即保存本地恢复记录；网络短暂中断自动重试一次，失败或中途停止后再次加入同一文件只补缺失段。
- 未完成 TXT 按原顺序保留已完成文字，并在缺失段原位置标记待补；补齐后生成完整 TXT。
- 增加“清除恢复缓存”入口。恢复记录在成功输出后自动删除，失败时保留以便继续。

## 1.1.0-preview.1 — 2026-09-23

- 增加 1～20 路可选并发任务池，默认 5 路；配置对之后加入的文件生效。
- 音频分段使用独立后台会话并行识别，再按时间顺序合并 TXT；失败时仅保留连续完成的前缀。
- Windows 临时文件被短暂占用时重试清理，清理仍失败则提示缓存路径，避免把完整转写误报为失败。
- 用公开的 NASA 视频音频进行实际转写验证；20 路高并发仍未验证。

## 1.0.0-preview.1 — 2026-09-20

首次公开预览版，基于本地 1.0.0 版本整理：

- 拖拽、多文件队列、托盘运行和 TXT 输出。
- 检测豆包输入法是否安装及组件是否适配。
- 分段在线识别、超时处理与同名输出保护。
- 公开源码、MIT 许可证、隐私和第三方依赖说明。
- 增加校验依赖的构建流程、离线回归测试和问题反馈模板。
- 修复重叠文本拼接时相邻标点重复的问题。

限制：仅验证固定的豆包输入法 0.9.0.0 组件；尚未在第二台物理电脑上实测；安装包无代码签名。
