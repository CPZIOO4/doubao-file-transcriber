# 1.0.0-preview.1 验证记录

日期：2026-09-20。本文记录本次发布实际执行的检查，不代表所有 Windows 电脑都可用。

| 检查 | 结果 | 范围 |
| --- | --- | --- |
| 官方依赖下载与 SHA-256 校验 | 通过 | Python 3.14.7、NAudio 1.10.0 从官方来源重新下载；使用现有 Inno Setup 6.7.3 编译器 |
| C# 编译、Python 后台及安装包构建 | 通过 | Windows x64；生成约 12 MB 安装包 |
| 6 项离线回归检查 | 通过 | 不覆盖输出、Unicode、重叠标点、83 秒 PCM 分段覆盖、会话不完整拒绝、缺失/未知组件拒绝、模拟网络失败后保留部分结果并清理缓存 |
| 本机组件检测 | 通过 | 找到指纹匹配的豆包输入法 0.9.0.0 |
| 真实在线转写 | 通过 | 使用 Windows 语音合成生成约 7 秒非敏感 WAV，实际调用已安装组件并得到 TXT |
| 正常结束缓存清理 | 通过 | 上述在线测试工作缓存目录为空 |
| GUI 启动与兼容性提示 | 通过 | 实际启动新构建窗口，检查可见布局、可用按钮和“已找到兼容”提示 |
| 安装包签名 | 未签名 | 不把 SHA-256 校验当作安全认证 |
| 安装器完整安装/卸载流程 | 本轮未验证 | 安装脚本已编译，未在本机覆盖已有安装来测试卸载 |
| 拖拽、取消、托盘恢复的完整交互 | 本轮未完成验收 | 源码已检查；不能用后端测试替代完整 UI 验收 |
| 第二台物理电脑 / Windows 10 / Windows N / 干净系统 | 未验证 | 需要后续独立环境测试 |
| 新版本豆包、服务端变更、长时间稳定性 | 未验证 | 仅适配已固定的组件版本 |

在线样本由本机语音合成生成，不是真人录音，内容为：

> 你好，这是我的语音标准文件。现在正在验证录音转写助手。

实际 TXT 与上述句子一致。录音、输出和内部测试目录不进入 Git 仓库或 Release。

发布检查包括待提交文件清单、常见凭据模式和个人路径扫描、源码人工阅读、干净打包目录清单检查。安装脚本明确只收入程序、说明、许可和运行时，不收入豆包二进制、测试文件或本机账户配置。这些检查不是完整安全审计，也不验证上游原生组件的全部行为。
